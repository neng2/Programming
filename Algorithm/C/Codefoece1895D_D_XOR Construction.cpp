#include <iostream>

#define fastio ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
#define MAX 200000
using namespace std;
int arr[MAX];
int brr[MAX];
pair<int,int> p;
int main(){
    int N; cin>>N;
    for(int i=0;i<N-1;i++){
        cin>>arr[i];
    }
}

/*

*/